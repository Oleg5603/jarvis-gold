# Статус активной сессии — читать в начале НОВОГО чата после падения

Обновляется по ходу работы. Если чат упал — прочитай этот файл первым делом,
прежде чем грепать транскрипты или спрашивать пользователя "с чего начать".

## 1. Jurist — оркестратор агентов: ЗАВЕРШЕНО
Всё сделано, протестировано, закоммичено и запушено в origin (репозиторий
`C:\Users\HP\Jurist`, ветка `master`, последний коммит на момент записи —
`c0506fe` "Document agent-orchestrator rollout in progress log").
Подробности: `C:\Users\HP\Jurist\PROGRESS.md`, секция "2026-07-15 — полноценный
оркестратор агентов + верификация цитат + шаблоны".
Сервер живой на `127.0.0.1:8010`, ngrok-туннель поднят на прежнем поддомене.
**Дальнейших действий не требуется**, если пользователь не попросит новое.

## 2. Транскрибация видео `соцсети 1.avi` — ЗАВЕРШЕНО (2026-07-16)
Транскрипт готов (лог дошёл до `DONE`), на его основе составлен план
ContentZavod — см. ветка `ContentZavod` в `C:\Users\HP\gavrik`, файлы
`content_zavod/PLAN.md`, `ROLES.md`, `CRITIQUE.md`, `SOURCE.md`.
Коммиты: `8120275`, `704ed8c`, `931f5a7`. Подробности в памяти
`project_content_zavod.md`. Открытый блокер перед Фазой 1: не заполнен
`clients/oleg/brand.md` (нужен ответ Олега — какой продукт/тема личного
бренда). Оригинальный текст ниже сохранён как история, актуальность см. выше.

<details><summary>Исходная запись (устарела)</summary>

Транскрибация видео `соцсети 1.avi` — В ПРОЦЕССЕ (фон)
Пользователь попросил (2026-07-15) вытащить из `C:\Users\HP\Desktop\соцсети 1.avi`
(~2ч28м, Bandicam-запись) подробный план создания "контент-завода" для ВК и Телеграм.

**Как устроено:**
- Аудио уже извлечено (mono 16kHz wav) — но лежит в session-scratchpad, который
  может не пережить рестарт чата. Если `socseti1.wav` пропал — просто извлеки
  заново командой (быстро, ~1 минута):
  ```
  ffmpeg -y -i "C:\Users\HP\Desktop\соцсети 1.avi" -vn -ac 1 -ar 16000 -acodec pcm_s16le "<новый путь>\socseti1.wav"
  ```
  (ffmpeg уже установлен через winget: `Gyan.FFmpeg`)
- Транскрибация идёт через `faster-whisper`, модель `"base"`, `device="cpu"`,
  `compute_type="int8"`, `language="ru"`, `vad_filter=True` — скорость примерно
  реального времени (не быстрее). Модель `"small"` пробовали первой — в 3+ раза
  медленнее, не годится, переключились на `"base"`.
- **ВАЖНО:** на этой машине системный прокси (Happ VPN) регистрируется как
  `socks=` в реестре Windows, что `httpx`/huggingface_hub автоопределяют как
  `socks4` (не поддерживается) → `ValueError`. Перед запуском ЛЮБОГО
  Python-скрипта, качающего что-то из интернета (включая veci whisper-модели),
  нужно явно выставить:
  ```
  export HTTPS_PROXY="socks5://127.0.0.1:10808"
  export HTTP_PROXY="socks5://127.0.0.1:10808"
  ```
  См. также этот же баг в `C:\Users\HP\Jurist\llm.py` (постоянная проблема на
  этой машине, не специфичная для транскрибации).
- Скрипт транскрибации: копия сохранена в
  `C:\Users\HP\Documents\Project\session-work\transcribe_script.py` (та же
  версия, что использовалась). Пишет прогресс построчно с таймкодами
  (`[123s] текст...`), можно смотреть на лету.
- **Живой лог прогресса синхронизируется сюда:**
  `C:\Users\HP\Documents\Project\session-work\socseti1_transcribe.log`
  (копия из session-scratchpad, обновляется при каждой проверке — смотри
  таймкод последней строки, чтобы понять текущий прогресс в секундах аудио;
  всего аудио ~8880 секунд).

**Если процесс транскрибации мёртв (проверить: `tasklist | grep python`,
искать процесс с ~400-500МБ+ памяти) и лог не растёт — перезапустить:**
```bash
cd "C:\Users\HP\Documents\Project\session-work"
export HTTPS_PROXY="socks5://127.0.0.1:10808"
export HTTP_PROXY="socks5://127.0.0.1:10808"
nohup python transcribe_script.py "<путь к socseti1.wav>" "socseti1_transcript.txt" > socseti1_transcribe.log 2>&1 &
disown
```
Транскрибация начнётся с нуля (whisper не поддерживает резюме с середины
файла) — это нормально, модель быстрая (~real-time), не критично.

**Когда транскрипт завершится** (в логе появится строка `DONE`): прочитать
`socseti1_transcript.txt` целиком, и на его основе составить подробный план
создания контент-завода для ВК и Телеграм — это ещё не сделано, финальная
задача пользователя, ради которой всё это затевалось.

</details>

## 4. VPS 159.194.200.172 (Beget "Funny Lars") — наведение порядка (Сценарий А) — В ПРОЦЕССЕ (2026-07-19)
Доступ: root / пароль лежит в `C:\Users\HP\Desktop\4.docx` (в тексте документа рядом с IP).
⚠️ Этот docx хранит открытым текстом root-пароль + несколько Telegram/GitHub токенов — не
удалён, не зашифрован, стоит перенести в менеджер паролей (см. `project_tkm_beget_deploy`
в памяти про ту же гигиену секретов).

**Сделано:** полный аудит (Шаг 1 промпта `prompt-server-setup-unified`) — до миграции ВСЕ
боты (`gavrik`, `sveta-bot`, `svet-bot`, `telegram-bot`, `misemia-bot`) жили под root в
`/root/`, без изоляции, ufw был выключен (`inactive`).

**Мигрирован `sveta-bot`** (первый и пока единственный) в изолированного юзера:
- юзер `sveta-bot` (uid 1001), код в `/home/sveta-bot/sveta-bot/`, свой venv
- токен вынесен из кода в `.env` (600), абсолютные `/root/...` пути в коде заменены на
  относительные (`BASE_DIR = Path(__file__).resolve().parent`)
- systemd-юнит `sveta-bot.service` теперь с `User=sveta-bot`, `NoNewPrivileges`,
  `ProtectSystem=strict`, `InaccessiblePaths=/root`, `MemoryMax=512M`
- старый код сохранён (не удалён) как `/root/sveta-bot.OLD-2026-07-19`
- была короткая просадка (~45 сек) при переключении из-за отсутствующего pip-экстра
  `python-telegram-bot[job-queue]` — почищено, сейчас стабильно работает (подтверждено
  `getMe`/`deleteWebhook` 200 OK и живым процессом)

**Следующий шаг:** остались `gavrik`, `svet-bot`, `telegram-bot` (сейчас все три
НЕ активны — юниты есть, но не запущены, риск ниже). Но **перед миграцией
`telegram-bot`/`misemia-bot` нужно сначала разобраться, что там вообще живое** —
это не отдельный бот, а старая/параллельная копия Гаврика (папка `/root/telegram-bot`,
юнит назывался "Telegram Claude Bot", внутри — форекс-модуль похожий на Штаб,
CRM лидов психотерапевта, голосовой ввод через whisper, `GAVRIK_MASTER.md`).
Юнит `telegram-bot.service` **уже отключён** — упал 2026-07-07 с ошибкой
`Conflict: terminated by other getUpdates request` (конфликт токена с другим
процессом, скорее всего с настоящим `gavrik.service`). `.env` в этой папке
содержит 4 токена сразу: `MISEMIA_BOT_TOKEN`, `TELEGRAM_BOT_TOKEN`, `LANA_BOT_TOKEN`,
`MISEMIA_VK_TOKEN` — прежде чем мигрировать/удалять, нужно понять, какие из них
всё ещё живые/нужные, а какие можно выключить насовсем.

**Как продолжить:** следующая сессия начинается с вопроса пользователю — "какие
боты в /root/telegram-bot реально нужны" — не мигрировать вслепую, там явная
путаница из нескольких ботов в одной папке. После ответа — план миграции для
следующего бота по той же схеме, что и sveta-bot выше (шаблон в
`Downloads/prompt-server-setup-unified (4).md`).

## 3. net-monitor — обрывы к api.anthropic.com — В ПРОЦЕССЕ
**Правка directSites/directIp НЕ помогла**: после правки (~18:23-18:30) обрывы
к Claude API продолжаются с той же/большей частотой (лог 2026-07-15, обрывы
каждые 6-15 минут вплоть до 22:57 на момент записи). Гипотеза "трафик шёл в
обход VPN" отклонена — теперь весь трафик и так глобальный прокси.

**Новая гипотеза (2026-07-15 ~23:00):** обрыв происходит на этапе TLS-
хендшейка (ошибка `_ssl.c:1063: handshake operation timed out` — TCP уже
установлен, рвётся именно негоциация TLS), причём **только для
api.anthropic.com**, а не для google.com/ya.ru (которые проверяются в цепочке
раньше и проходят). Это указывает на точечные потери пакетов на пути
VPN-туннель → конкретный Cloudflare PoP, к которому anycast приводит
api.anthropic.com — а не на общую проблему VPN-роутинга.
Проверено: `curl -4` к api.anthropic.com сейчас проходит нормально (CF-RAY
показал дата-центр ORD/Chicago). `curl -6` не резолвит вовсе — но это
системная особенность (машина не имеет реального IPv6-маршрута), не
специфично для Anthropic, скорее всего не связано с обрывами (обрывы — это
handshake timeout, а не resolve failure).

**Следующий шаг, если продолжать:** поймать обрыв в реальном времени и
снять `tracert`/`mtr` до 160.79.104.10 в момент сбоя, либо посмотреть, меняет
ли переключение VPN-сервера/протокола в Happ частоту обрывов. Не доказано
на 100% — рабочая гипотеза, не подтверждённый факт.

**Отложено на 2026-07-16 (следующий раз, когда есть силы):** пользователь
попросил "другой путь" для Claude API вместо флакового Happ VPN-туннеля,
но устал разбираться прямо сейчас. Два варианта на выбор, ничего ещё не
сделано:
1. Переключить сервер/протокол внутри Happ (просто, без установки ПО) —
   и проверить через net-monitor/watcher, помогло ли.
2. Поставить Cloudflare WARP как отдельный туннель только для
   api.anthropic.com (split-tunnel), независимый от Happ — требует
   установки нового ПО, нужно подтверждение перед установкой.
Трассы (`dropout_traces/`) показали: `tracert` видит только 1 хоп (VPN
скрывает реальный путь), `curl` не может установить TCP-соединение к
160.79.104.10 за 10с в момент обрыва — то есть баг где-то внутри самого
туннеля Happ к этому конкретному IP, не в общей магистрали (google/ya.ru
почти всегда живы одновременно).

**Запущен watcher (2026-07-15 ~23:05):**
`C:\Users\HP\Documents\Project\session-work\catch_dropout.py` работает в
фоне (запущен через `nohup ... > catch_dropout.log 2>&1 &`, `disown`). Следит
за `net-monitor/net_monitor_log.txt`; при появлении новой строки
"НАЧАЛО ОБРЫВА" по Claude API сразу снимает `tracert -4` и `curl -4 -v` до
api.anthropic.com и сохраняет в
`C:\Users\HP\Documents\Project\session-work\dropout_traces\trace_<таймкод>.txt`.
Если чат упал — проверить, жив ли процесс (`tasklist | grep python`, искать
среди PID, не совпадающих с транскрибацией) и есть ли новые файлы в
`dropout_traces\`. Если файлы появились — прочитать их и сопоставить с
временем обрыва в `net_monitor_log.txt`, чтобы понять, на каком хопе рвётся
трасса или сколько занимает TLS-подключение (`time_appconnect` в выводе curl).
Если процесс мёртв — перезапустить той же командой.

## Как использовать этот файл в новом чате
1. Прочитать целиком.
2. Проверить актуальность каждого раздела (git log в Jurist, `tasklist`,
   свежие строки в логах) — этот файл может отставать от реальности.
3. Продолжить с того пункта, который ещё не завершён (сейчас — раздел 2 и
   финальный вывод раздела 3).
